name: LinkedIn Debug

on:
  workflow_dispatch:

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Debug 1 - reached scraping step
        run: echo "Scraping step started"

      - name: Scrape LinkedIn followers
        run: |
          echo "About to run Puppeteer"
          node -v
          npm -v
          ls -la
